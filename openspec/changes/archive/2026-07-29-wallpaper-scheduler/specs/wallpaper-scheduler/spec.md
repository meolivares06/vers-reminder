# Wallpaper Scheduler

## ADDED Requirements

_R-WS-001_ through _R-WS-008_ and scenarios WS-01–WS-08 as defined in `openspec/specs/wallpaper-scheduler/spec.md`. This is a NEW capability — no existing spec was modified.
