# OpenSpec config — vers-reminder
# Auto-generated by sdd-init

context: >
  Flutter Android app for memorizing Bible verses. Combines verses with
  nature wallpapers using a background automation engine. PRD approved
  in prd.md. Stack per PRD: Flutter/Dart, isar/hive, dio, workmanager,
  wallpaper_manager_flutter. No Flutter project scaffolded yet; this
  config defines conventions for when code generation starts.

strict_tdd: false
strict_tdd_reason: >
  No test runner detected — project has no pubspec.yaml or test config
  yet. Once Flutter is scaffolded with flutter_test, set strict_tdd: true.

testing:
  runner: null
  frameworks: []
  layers:
    unit: false
    integration: false
    e2e: false
  coverage: false
  linter: false
  type_checker: false
  formatter: false

artifacts_mode: openspec
execution_mode: interactive
pr_strategy: ask
review_budget: 400

phases:
  proposal: enabled
  spec: enabled
  design: enabled
  tasks: enabled
  apply: enabled
  verify: enabled
  archive: enabled

agent_rules:
  - "Always check openspec/config.yaml for project conventions before starting work"
  - "Read the full PRD (prd.md) before proposing changes"
  - "Do not write code before the user confirms the change via sdd-propose"
  - "Keep PRs under 400 lines (review budget D1)"
  - "Ask before creating any PR (strategy C1)"
